allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    version = '14.0'
}

// include xfunction for updateVersion
buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "io.github.lambdaprime:id.xfunction:14.0"
    }
}
